/** Placeholder – ActiveCollab connection flow. */
export function ActiveCollabConnectionFlow() {
  return <p className="text-sm text-muted-foreground py-4">ActiveCollabConnectionFlow (placeholder)</p>;
}
