/**
 * Project duplicate detection - per PROJECTS-EXACT-FILE-LIST.
 */
export function detectDuplicateProjects(_projects: { name: string; slug: string }[]): { name: string; slug: string }[] {
  return [];
}
